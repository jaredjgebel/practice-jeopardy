{
  "name": "practice-jeopardy",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "yarn --cwd server && yarn --cwd client",
    "build": "yarn --cwd client build"
  },
  "engines": {
    "yarn": "1.x",
    "node": "10.x"
  }
}
