import * as c from "../constants";

const defaultState = {
  cluesById: {},
  allIds: []
};

const clues = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default clues;
